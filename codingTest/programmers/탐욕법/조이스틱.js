console.log(solution('JEEEAAAHTTTERWOEN'));

function solution(name) {
    var answer = 0;
    arr = name.split('');
    console.log(arr.indexOf('A'));
    console.log(arr.lastIndexOf('A'));

    for (let i=0;i<arr.length;i++) {

        
        //console.log(Math.abs(arr[i].charCodeAt(0)-'A'.charCodeAt(0)));
    }
    return answer;
}